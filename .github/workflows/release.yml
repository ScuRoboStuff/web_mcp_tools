name: release (deprecated)

# Deprecated: superseded by .github/workflows/deploy.yml which
# performs CI -> Semantic Release -> CDK deploy in one pipeline.
# This workflow is disabled and kept only for reference.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use .github/workflows/deploy.yml instead." && exit 0
